{
    "builds": [{
        "src": "usforces/wsgi.py",
        "use": "@ardnt/vercel-python-wsgi",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "usforces/wsgi.py"
        }
    ]
}